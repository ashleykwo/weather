import React from 'react';

const hourlyForecast = (props) => {
  /*
  const hourlyData = props.hourly.data.map(item => {
    return (
      <li>
        <p>{item.time}</p>
        <p>{item.temperature}</p>
      </li>
    )
  });
  */
  

  return(
    <div className="container">
    </div>
  )
}

export default hourlyForecast;